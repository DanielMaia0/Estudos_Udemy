// (condição) ? 'Valor para verdadeiro' : 'Valor para falso';

const point = 999;
const nivel = point >= 1000 ? "Usuário VIP" : "Usuário Normal";


const cor = null;
const corpadrao = cor || 'preta';

console.log(nivel, corpadrao)



/*
if (point >= 1000) {
    console.log('Usuário VIP');
} else {
    console.log('Usuário Normal');
} */


